# ShengWen 功能更新说明

本文档描述了最近添加的新功能和配置选项。

## 新增功能

### 1. YouTube 字幕直取功能（两级方案）

**问题**: 下载 YouTube 视频后再转录速度慢，YouTube 本身提供字幕接口

**解决方案**:
- 实现两级字幕提取方案：
  1. **优先**: `youtube-transcript-api` — 直接调 YouTube 内部 transcript 端点，更快更轻
  2. **降级**: `yt-dlp` — transcript-api 失败时降级使用，走 player 端点
- 在 `config/settings.json` 中新增配置项:
  ```json
  {
    "whisper": {
      "enable_youtube_subtitle_fetch": true  // 是否启用 YouTube 字幕直取
    }
  }
  ```
- 默认启用（`true`）
- 自动选择中文字幕（简体/繁体），无中文字幕时自动回退到英文字幕
- 如果视频未启用字幕或字幕接口不可用，自动降级到下载+ASR流程

**安装依赖**:
```bash
pip install youtube-transcript-api
```

**技术细节**:
- 支持标准 YouTube URL: `https://www.youtube.com/watch?v=VIDEO_ID`
- 支持短链: `https://youtu.be/VIDEO_ID`
- 优先选择 AI 生成字幕（`is_generated`），无 AI 字幕时选择人工字幕
- 字幕格式化为 HHMMSS 时间戳 + 文本，与现有转录格式一致
- 支持繁简转换（如果启用）

---

### 2. 繁简转换功能

**问题**: B 站字幕提取后全部是繁体字

**解决方案**:
- 添加了 `opencc` 依赖，用于繁体到简体中文的转换
- 在 `config/settings.json` 中新增配置项:
  ```json
  {
    "whisper": {
      "convert_traditional_to_simplified": true  // 是否将繁体字幕转换为简体
    }
  }
  ```
- 默认启用转换（`true`）
- 如果 `opencc` 未安装，系统会记录警告但继续运行（字幕保持原样）

**安装依赖**:
```bash
pip install opencc
```

---

### 3. AI 总结开关功能

**问题**: 有时候不需要 AI 总结，只需要转录

**解决方案**:
- 添加了 `enable_summarization` 配置项
- 在 `config/settings.json` 中:
  ```json
  {
    "summarization": {
      "enable_summarization": true  // 是否启用 AI 总结
    }
  }
  ```
- 当设置为 `false` 时，任务在转录完成后直接标记为完成，跳过 AI 总结步骤
- 前端可通过设置面板控制此选项

**工作流程**:
- 启用总结: 下载 → 转录 → AI 总结 → 完成
- 禁用总结: 下载 → 转录 → 完成

---

### 4. 文件保存路径配置

**问题**: 用户希望自定义原文和总结文件的保存位置

**解决方案**:
- 在 `config/settings.json` 中新增 `output` 配置段:
  ```json
  {
    "output": {
      "transcript_dir": "temp",      // 转录文件（字幕）保存目录
      "summary_dir": "temp"          // 总结文件保存目录
    }
  }
  ```
- 支持相对路径（相对于项目根目录）或绝对路径
- 目录不存在时会自动创建
- 前端设置面板可配置这两个路径

**默认值**:
- `transcript_dir`: `temp`
- `summary_dir`: `temp`

---

## 配置文件示例

完整的 `config/settings.json` 示例（包含所有新配置）:

```json
{
  "app": {
    "host": "0.0.0.0",
    "port": 8000,
    "enable_progress_test": false,
    "enable_mdns": false,
    "frontend_dist_dir": "frontend/dist",
    "prompt_file": "src/main/python/sheng_wen/prompt.md"
  },
  "whisper": {
    "model_source": "auto_download",
    "model_path": null,
    "model_size": "tiny",
    "device": "cpu",
    "enable_bilibili_subtitle_fetch": true,
    "enable_youtube_subtitle_fetch": true,
    "bilibili_sessdata": "",
    "faster_whisper_model_path": null,
    "convert_traditional_to_simplified": true
  },
  "llm": {
    "provider": "openai_compatible",
    "base_url": "",
    "api_key": "",
    "model_id": "",
    "temperature": 0.7,
    "context_window_size": 1000000
  },
  "database": {
    "sqlite_path": "ShengWen.db",
    "json_file_path": "tasks.json"
  },
  "cors": {
    "allow_origins": "*",
    "allow_credentials": true,
    "allow_methods": "*",
    "allow_headers": "*"
  },
  "summarization": {
    "mode": "auto",
    "auto_chunk_min_audio_duration_sec": 2400,
    "auto_chunk_min_transcript_lines": 1800,
    "chunk_target_duration_sec": 2400,
    "chunk_min_duration_sec": 1800,
    "chunk_max_duration_sec": 3000,
    "boundary_jump_sec": 10,
    "prev_tail_timestamp_lines_m": 10,
    "prev_summary_tail_chars_j": 500,
    "llm_call_retry_max": 3,
    "fallback_to_standard_on_agent_error": true,
    "chunk_prompt_file": "src/main/python/sheng_wen/prompt_for_chunk.md",
    "max_agent_value_chars": 500,
    "chunk_debug_dump_enabled": false,
    "chunk_debug_dump_dir": "temp/chunk_debug",
    "enable_agent_pipeline": false,
    "transcript_chunk_emit_duration_sec": 600,
    "enable_summarization": true
  },
  "output": {
    "transcript_dir": "temp",
    "summary_dir": "temp"
  }
}
```

---

## 前端配置

用户可以通过前端设置面板配置以下新选项：

1. **转录设置** → YouTube 字幕直取
   - 开关：是否启用 YouTube 字幕直取（优先 youtube-transcript-api，失败时回退 yt-dlp）

2. **转录设置** → 繁简转换
   - 开关：是否将繁体字幕转换为简体中文

3. **Agent 设置** → AI 总结
   - 开关：是否启用 AI 总结

4. **Agent 设置** → 文件保存路径
   - 转录文件目录：自定义字幕/原文保存位置
   - 总结文件目录：自定义 AI 总结文件保存位置

---

## 注意事项

1. **依赖安装**: 
   - 确保运行 `pip install -r requirements.txt` 以安装 `opencc`

2. **目录权限**:
   - 确保 Python 进程有权限写入配置的输出目录

3. **配置持久化**:
   - 所有配置变更会自动保存到 `config/settings.json`
   - 重启服务后配置仍然有效

4. **OpenCC 可选**:
   - 如果未安装 `opencc`，系统会记录警告但不影响核心功能
   - 建议安装以获得更好的字幕阅读体验

---

## 技术细节

### 繁简转换实现
- 使用 `OpenCC` 库的 `t2s` 模式（繁体转简体）
- 在 B 站字幕提取后立即转换
- 转换前会检查 `convert_traditional_to_simplified` 配置

### AI 总结跳过逻辑
- 在 `VideoDownloaderWorker` 和 `TranscriberWorker` 中添加了判断
- 禁用总结时，任务状态直接从 `TRANSCRIBING` 变为 `COMPLETED`
- 不调用 `summary_worker`，节省 API 调用成本

### 输出路径处理
- 通过 `_get_output_dir()` 辅助方法统一处理
- 支持相对路径和绝对路径
- 自动创建不存在的目录
- 文件名使用 `task_id` 作为前缀，确保唯一性
